// HRM module temporarily disabled — see AppLayout.tsx's commented-out "HRMS" nav group.
// import { createFileRoute } from "@tanstack/react-router";
// import { HrDashboard } from "@/components/hr/HrDashboard";
//
// export const Route = createFileRoute("/hrms/dashboard")({
//   head: () => ({ meta: [{ title: "HR Dashboard — BuildPOS" }, { name: "description", content: "Workforce operations across branches: attendance, shifts, leave, contracts and activity." }] }),
//   component: HrDashboard,
// });
